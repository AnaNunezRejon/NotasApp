<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotasApp">

        <!-- MAIN -->
        <activity
            android:name=".view.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- REGISTRO -->
        <activity
            android:name=".view.activities.RegistroActivity"
            android:exported="false" />

        <!-- HOME -->
        <activity
            android:name=".view.activities.HomeActivity"
            android:exported="false" />

        <!-- DATOS PERSONALES -->
        <activity android:name=".view.activities.DatosPersonalesActivity" />

        <!-- CAMBIAR CONTRASEÑA -->
        <activity android:name=".view.activities.CambiarContrasenaActivity"
            android:exported="false" />

        <!-- NOTA -->
        <activity android:name=".view.activities.NotaActivity"
            android:exported="false"/>

        <!-- NOTA ALEATORIA -->
        <activity
            android:name=".view.activities.NotaAleatoriaActivity"
            android:exported="false" />

    </application>

</manifest>
